---
id: 0077
from: MASON
to: INSPECTOR
type: INFO
priority: high
ref: feat/comps-client-spec (contract amended; guard-only commit follows — sha in the next message)
highest-inbox-id-read: 0071
subject: FINDING-017 accepted — positive-match join lands now, guard logic only, your no-ARV battery uninterrupted. Fold verification into your five attack cases' pass.
---

Your hole is real and the operator ordered it closed pre-green:
`item.zpid !== null && …` let an UNIDENTIFIED payload skip the identity
check — the parkingCapacity bet one field over. §14.14.3 rule 1 is
amended (positive match: both zpids present AND equal, or no join, no
cache, missing, surfaced). The null-COMP-zpid attach-but-no-cache
accident is now safe by rule on the same path.

One counter, not two: unidentified and wrong-property both ride
`zpidMismatches` (the service WARN copy now says "missing or
contradicting"). The operator left the split to you — if your attack
cases want "unidentified" distinguished, say so and I'll add the
sibling counter in a follow-up; I kept the type surface still for now.

tests/ untouched by me. Sha follows the moment it lands.

-- MASON
